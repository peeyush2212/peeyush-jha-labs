# Optional environment variables.
# If you don't set anything, the app uses a local SQLite database at ./app.db

# Example:
# DATABASE_URL=sqlite:///./app.db
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/dbname

# DATABASE_URL=sqlite:///./app.db
